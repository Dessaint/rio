{% extends 'base.html.twig' %}

{% block body %}
    <h1>Epreuves</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ epreufe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ epreufe.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ epreufe.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ epreufe.picture }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('epreuves_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('epreuves_edit', { 'id': epreufe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
