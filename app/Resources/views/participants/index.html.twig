{% extends 'base.html.twig' %}

{% block body %}
    <h1>Participants list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participant in participants %}
            <tr>
                <td><a href="{{ path('participants_show', { 'id': participant.id }) }}">{{ participant.id }}</a></td>
                <td>{{ participant.nom }}</td>
                <td>{{ participant.prenom }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('participants_show', { 'id': participant.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('participants_edit', { 'id': participant.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('participants_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
